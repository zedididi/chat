<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.ncu.bootwebsocketmybatis.dao.UserDao">
    <resultMap id="userMap" type="User">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="createtime" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <select id="findAll" resultMap="userMap">
        SELECT  id,username,password,createtime
        from user
    </select>
    <select id="findById" resultMap="userMap">
        select id,username,password,createtime
        from user
        where id=#{id,jdbcType=VARCHAR}
    </select>

    <insert id="addUser" keyProperty="id" parameterType="cn.edu.ncu.bootwebsocketmybatis.entity.User">
        insert into user(id,username,password) values (#{id,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR});
    </insert>

    <update id="updateUser" keyProperty="id" parameterType="User">
        update user set username=#{userName,jdbcType=VARCHAR},password=#{password,jdbcType=VARCHAR}  where id=#{id,jdbcType=VARCHAR}
    </update>



</mapper>